input:
  download_input_path: '../data'
  resample_scale_factor: 2
train:
  batch_size: 4
  learning_rate: 0.0001
  epochs: 50
  optimizer:
    name: adam  # or 'sgd'
    # Additional parameters for SGD (optional):
    # momentum: 0.9
    # dampening: 0.2
    weight_decay: 1e-5
  log_loss_every_nth_batch: 5 #default 5
  validate_every_nth_batch: 2 #default 2
fine_tune:
  batch_size: 4
  learning_rate: 0.0001
  epochs: 2
evaluate:
  batch_size: 4
  eval_score_path: '../eval_score'
visualize:
  vis_num_images: 4
  vis_save_path: '../comparison_model_output_images'
  max_vis: 10
dataset:
  subset_percentage: 0.9
